package org.connect4.messages;

/**
 * The Enum MessageType.
 */
public enum MessageType {

    /** The login request. */
    LOGIN_REQUEST,
    /** The login response. */
    LOGIN_RESPONSE,
    /** The notification sign in. */
    NOTIFICATION_SIGN_IN,
    /** The notification sign out. */
    NOTIFICATION_SIGN_OUT,
    /** The public. */
    PUBLIC,
    /** The on line users request. */
    ON_LINE_USERS_REQUEST,
    /** The on line users response. */
    ON_LINE_USERS_RESPONSE,

    /** The private. */
    PRIVATE,
    /** The game. */
    GAME,
    /** The create acount request. */
    CREATE_ACOUNT_REQUEST,
    /** The create acount response. */
    CREATE_ACOUNT_RESPONSE;

}
